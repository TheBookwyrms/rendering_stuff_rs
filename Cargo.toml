[package]
name = "rendering_stuff_rs"
version = "1.0.0"
edition = "2024"

# run `cargo depgraph --all-deps` to get graphviz source which can graph all dependencies


[dependencies]
numeracy = {path = "../numeracy"}
#numeracy = {git = "https://github.com/TheBookwyrms/numeracy.git", branch = "working_branch"}
atmospheric = {path = "../atmospheric"}
#atmospheric = {git = "https://github.com/TheBookwyrms/atmospheric.git", branch = "working_branch"}

#ppm_viewer = {path = "ppm_viewer"}
ray_tracer = {path = "ray_tracer"}
